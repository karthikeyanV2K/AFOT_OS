<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.afot.locksystem">

    <!-- System and Security Permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCREEN_DIM_WAKE_LOCK" />
    <uses-permission android:name="android.permission.DEVICE_POWER" />
    
    <!-- Biometric Permissions -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Device Admin Permissions -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />
    <uses-permission android:name="android.permission.MANAGE_DEVICE_ADMINS" />
    
    <!-- System UI Permissions -->
    <uses-permission android:name="android.permission.STATUS_BAR" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    
    <!-- Phone and Emergency -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CALL_PRIVILEGED" />
    
    <!-- Location for weather -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- Network for weather updates -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Notification Access -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
    
    <!-- AFOT System Permissions -->
    <uses-permission android:name="com.afot.permission.LOCK_SYSTEM" />
    <uses-permission android:name="com.afot.permission.BIOMETRIC_ADVANCED" />
    <uses-permission android:name="com.afot.permission.SYSTEM_OVERLAY" />

    <!-- Hardware Features -->
    <uses-feature
        android:name="android.hardware.fingerprint"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />

    <application
        android:name=".AFOTLockApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AFOTLockSystem"
        tools:targetApi="31">

        <!-- Main Activity (Settings) -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AFOTLockSystem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lock Screen Activity -->
        <activity
            android:name=".ui.activity.LockScreenActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:showOnLockScreen="true"
            android:theme="@style/Theme.AFOTLockSystem.Fullscreen"
            android:turnScreenOn="true" />

        <!-- Setup Activity -->
        <activity
            android:name=".ui.activity.SetupActivity"
            android:exported="false"
            android:label="@string/setup_lock_system"
            android:theme="@style/Theme.AFOTLockSystem" />

        <!-- Biometric Setup Activity -->
        <activity
            android:name=".ui.activity.BiometricSetupActivity"
            android:exported="false"
            android:label="@string/biometric_setup"
            android:theme="@style/Theme.AFOTLockSystem" />

        <!-- Emergency Activity -->
        <activity
            android:name=".ui.activity.EmergencyActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.AFOTLockSystem.Emergency" />

        <!-- Lock Screen Service -->
        <service
            android:name=".service.LockScreenService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="systemExempted"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            
            <intent-filter>
                <action android:name="com.afot.locksystem.LOCK_SCREEN_SERVICE" />
            </intent-filter>
        </service>

        <!-- Device Admin Service -->
        <service
            android:name=".service.DeviceAdminService"
            android:enabled="true"
            android:exported="false" />

        <!-- Biometric Service -->
        <service
            android:name=".service.BiometricService"
            android:enabled="true"
            android:exported="false" />

        <!-- Weather Service -->
        <service
            android:name=".service.WeatherService"
            android:enabled="true"
            android:exported="false" />

        <!-- Notification Listener Service -->
        <service
            android:name=".service.NotificationListenerService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Device Admin Receiver -->
        <receiver
            android:name=".receiver.DeviceAdminReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
        </receiver>

        <!-- Screen State Receiver -->
        <receiver
            android:name=".receiver.ScreenStateReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Security Receiver -->
        <receiver
            android:name=".receiver.SecurityReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Lock Screen Widget Provider -->
        <receiver
            android:name=".widget.LockScreenWidgetProvider"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lock_widget_info" />
        </receiver>

        <!-- Tile Service for Quick Settings -->
        <service
            android:name=".service.LockTileService"
            android:enabled="true"
            android:exported="true"
            android:icon="@drawable/ic_lock"
            android:label="@string/afot_lock"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Accessibility Service (for system overlay) -->
        <service
            android:name=".service.AccessibilityService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Content Provider for settings -->
        <provider
            android:name=".data.provider.LockSettingsProvider"
            android:authorities="${applicationId}.locksettings"
            android:enabled="true"
            android:exported="false" />

        <!-- Job Scheduler for background tasks -->
        <service
            android:name=".service.BackgroundTaskService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!-- Work Manager Initialize -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
